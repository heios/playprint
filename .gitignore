# macOS
.DS_Store

# Local reference material — kept locally, not committed
.references/

# Dependencies
node_modules/

# Build output
dist/
build/

# Local agent tooling config
.claude/

# Local-only process / operating doc (not shipped) — kept out of the repo for now
DEV_ORCHESTRATION.md
